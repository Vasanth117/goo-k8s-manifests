apiVersion: v1
kind: Service
metadata:
  name: goo-frontend-service
  labels:
    app: goo-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: goo-frontend
