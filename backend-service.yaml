apiVersion: v1
kind: Service
metadata:
  name: goo-backend-service
  labels:
    app: goo-backend
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  selector:
    app: goo-backend
